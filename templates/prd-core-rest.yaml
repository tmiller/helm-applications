{{- range $index, $customer := .Values.prd.core.rest.customers }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.prd.core.rest.environment }}-{{ $.Values.prd.core.rest.application }}-{{ $customer }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    version: {{ $.Values.prd.core.rest.application }}-{{ $.Values.prd.core.rest.image.version }}
  annotations:
    rgocd.argoproj.io/sync-wave: "{{ mod $index 2 }}"
spec:
  project: default
  source:
    repoURL: {{ $.Values.prd.core.rest.helm.gitRepository }}
    targetRevision: {{ $.Values.prd.core.rest.helm.gitRevision }}
    path: .
    helm:
      values: >-
        {{ $.Values.prd.core.rest | toJson }}
  destination:
    namespace: {{ $.Values.prd.core.rest.environment }}-{{ $customer }}
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true   
    syncOptions:
      - RespectIgnoreDifferences=true
      - CreateNamespace=true
{{- end }}
