{{- range $index, $customer := .Values.prd.legacy.customers }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.prd.legacy.environment }}-{{ $.Values.prd.legacy.application }}-{{ $customer }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    rgocd.argoproj.io/sync-wave: "{{ mod $index 2 }}"
spec:
  project: default
  source:
    repoURL: {{ $.Values.prd.legacy.helm.gitRepository }}
    targetRevision: {{ $.Values.prd.legacy.helm.gitRevision }}
    path: .
    helm:
      values: >-
        {{ $.Values.prd.legacy | toJson }}
  destination:
    namespace: {{ $.Values.prd.legacy.environment }}-{{ $customer }}
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true   
    syncOptions:
      - RespectIgnoreDifferences=true
      - CreateNamespace=true
{{- end }}
