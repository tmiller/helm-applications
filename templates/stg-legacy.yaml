{{- range $index, $customer := .Values.stg.legacy.customers }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $.Values.stg.legacy.environment }}-{{ $.Values.stg.legacy.application }}-{{ $customer }}
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    rgocd.argoproj.io/sync-wave: "{{ mod $index 2 }}"
spec:
  project: default
  source:
    repoURL: {{ $.Values.stg.legacy.helm.gitRepository }}
    targetRevision: {{ $.Values.stg.legacy.helm.gitRevision }}
    path: .
    helm:
      values: >-
        {{ $.Values.stg.legacy | toJson }}
  destination:
    namespace: {{ $.Values.stg.legacy.environment }}-{{ $customer }}
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true   
    syncOptions:
      - RespectIgnoreDifferences=true
      - CreateNamespace=true
{{- end }}
